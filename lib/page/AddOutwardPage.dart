import 'package:flutter/material.dart';

class AddOutwardPage extends StatelessWidget {
  const AddOutwardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Add Outward")),
      body: const Center(child: Text("Add Outward Page")),
    );
  }
}
