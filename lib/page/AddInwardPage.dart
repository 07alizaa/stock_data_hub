import 'package:flutter/material.dart';

class AddInwardPage extends StatelessWidget {
  const AddInwardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Add Inward")),
      body: const Center(child: Text("Add Inward Page")),
    );
  }
}
